{% extends 'base.twig' %}

{% block title %}Sign-up{% endblock %}

{% block content %}
    <form action="{{ formAction }}" method="{{ formMethod }}" accept-charset="utf-8">


        <label for="email"><b>Email: </b></label>
        <input type="text" name="email" value="{% if formData.email %}{{ formData.email }}{% endif %}">
        {% if formErrors.email %}
            <p style="color: red;">{{ formErrors.email }}</p>
        {% endif %}




        <label for="passwd"><b>Password: </b></label>
        <input type="password" name="passwd" value="{% if formData.passwd %}{{ formData.passwd }}{% endif %}">
        <!--No s'hauria de guardar la contra però l'enunciat diu que tot-->


        <label for="passwd2"><b>Repeat: </b></label>
        <input type="password" name="passwd2" value="{% if formData.passwd2 %}{{ formData.passwd2 }}{% endif %}">
        {% if formErrors.passwd %}
            <p style="color: red;">{{ formErrors.passwd }}</p>
        {% endif %}
        <!--No s'hauria de guardar la contra però l'enunciat diu que tot. Estic trist ^^'.-->

        <label for="birth">Birthdate (optional): </label>
        <input type="date" id="birth" name="birth" value="{% if formData.birth %}{{ formData.birth }}{% endif %}">
        {% if formErrors.birth %}
            <p style="color: red;">{{ formErrors.birth }}</p>
        {% endif %}



        <input type="submit" value="Send">
    </form>
{% endblock %}